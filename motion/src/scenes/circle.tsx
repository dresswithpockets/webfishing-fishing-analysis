import {Circle, makeScene2D, Code, Rect, Txt, lines} from '@motion-canvas/2d';
import {all, createRef, DEFAULT, spawn, waitFor} from '@motion-canvas/core';

export default makeScene2D(function* (view) {
    
})
